swagger: '2.0'
info:
  title: Minimal Case of REQ-E001 Rule
  version: '1.0'
paths:
  /endpoint:
    get:
      responses:
        default:
          description: ''
          schema:
            properties:
              property:
                type: string
                enum:
                - v1
            type: object
            x-model: get_endpoint_response

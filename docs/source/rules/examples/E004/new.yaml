swagger: '2.0'
info:
  title: Minimal Case of E004 Rule
  version: '1.0'
paths:
  /endpoint:
    post:
      parameters:
      - in: body
        name: body
        required: true
        schema:
          properties:
            property:
              type: string
              enum:
              - v1
          type: object
      responses:
        default:
          description: ''
